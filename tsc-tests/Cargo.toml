[package]
name = "tsc-tests"
version = "0.1.0"
edition = "2024"
description = "TSC compatibility testing and benchmarking for zelda"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
rayon = "1"
colored = "2"
