const greeting: string = "Hello, world!";
const count: number = 42;

function add(a: number, b: number): number {
    return a + b;
}

interface User {
    name: string;
    age: number;
}

const user: User = {
    name: "Alice",
    age: 30,
};
